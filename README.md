# weather-react

# currently no migrations present therefore run query

CREATE TABLE cities 
(
  id serial NOT NULL,
  city_name CHARACTER VARYING(50) NOT NULL,
  PRIMARY KEY (id)
);


npm install
npm run serve

